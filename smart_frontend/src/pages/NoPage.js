const NoPage = () => {
    return (
        <div>
            <h1>Development Page</h1>
            <p>Click the links above to go page to page</p>
            <p>In actual deployment Home Page will be OnLoad, this can be repurposed as an Error page as well... Try typing whatever at the end of the URL</p>
        </div>
    )
  };
  
  export default NoPage;